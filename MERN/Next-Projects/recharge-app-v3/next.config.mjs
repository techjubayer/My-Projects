/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "faruki.rechargeland.in",
      "imagedelivery.net",
      "images.ixigo.com",
      "images.unsplash.com",
      "app.billhub.in",
      "rechargediscount.in",
    ],
  },
};

export default nextConfig;
